extends base

block content
    main.main 
        div.card-container

            each tour in tours
                div.card
                    div.card__header
                        .card__picture 
                            .card__picture-overlay &nbsp;
                            img.card__picture-img(src=`img/tours/${tour.imageCover}` alt= `${tour.name}`) 
                        h3.heading-tertirary
                            span #{tour.name}

                    .card__details 
                        h4.card__sub-heading #{tour.difficulty} #{tour.duration}-day tour
                        p.card__text #{tour.summary}
                        div.card__data
                            svg.card__icon
                                use(xlink:href="img/icons.svg#icon-map-pin")
                            span #{tour.startLocation.description}
                        div.card__data
                            svg.card__icon
                                use(xlink:href="img/icons.svg#icon-calendar")
                            span= tour.startDates[0].toLocaleDateString('pt-BR', {day: 'numeric', month: 'long', year: 'numeric'})
                        div.card__data
                            svg.card__icon
                                use(xlink:href="img/icons.svg#icon-flag")
                            span #{tour.locations.length} paradas
                        div.card__data
                            svg.card__icon
                                use(xlink:href="img/icons.svg#icon-user")
                            span #{tour.maxGroupSize} pessoas

                    .card__footer
                        p
                            span.card__footer-value R$ #{tour.price} 
                            span.card__footer-text por pessoa
                        p.card__ratings
                            span.card__footer-value #{tour.ratingsAverage.toFixed(1)} 
                            span.card__footer-text avaliações (#{tour.ratingsQuantity})
                        a.btn.btn--green.btn--small(href=`/tour/${tour.slug}`) Detalhes



